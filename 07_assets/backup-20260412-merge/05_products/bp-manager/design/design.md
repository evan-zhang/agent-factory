# BP Manager 设计文档

## 产品定位

BP Manager 是面向管理者和员工的 BP 日常管理助手，通过自然语言交互帮助用户高效管理 BP。

## 核心能力

### 1. BP 查看
- 查看自己的 BP（通过员工ID自动定位）
- 查看下属的 BP（通过姓名搜索）
- 查看任意分组的 BP（通过分组ID）

### 2. BP 管理
- 新增关键成果（挂在目标下）
- 新增关键举措（挂在关键成果下）
- 注：编辑和删除操作需要通过 Web UI 完成

### 3. AI 检查
- 基于康哲规则检查 BP 质量
- 检查项：结构完整性、衡量标准、承接关系、举措可执行性、时间合理性

### 4. 延期提醒
- 手动触发，向指定员工发送延期提醒汇报
- 支持自定义提醒内容

### 5. 汇报查看
- 查看任务关联的汇报历史
- 分页查询，支持关键词搜索

### 6. 搜索功能
- 按名称搜索任务
- 按名称搜索分组

---

## 用户场景

### 场景一：管理者查看下属 BP
```
用户：查看张三的 BP
助手：
1. 搜索分组"张三"
2. 获取分组 BP Markdown
3. 输出 BP 内容
4. （可选）基于康哲规则给出建议
```

### 场景二：员工新增关键成果
```
用户：为目标 A4-1 新增一个关键成果：完成全栈交付项目模式探索的报告
助手：
1. 确认目标ID
2. 调用新增关键成果接口
3. 确认创建成功
```

### 场景三：发送延期提醒
```
用户：给李四发一个延期提醒，他的关键成果延期了
助手：
1. 确认员工ID和任务信息
2. 生成提醒内容
3. 调用发送延期提醒接口
4. 确认发送成功
```

---

## 技术架构

### 模块划分
```
bp-manager/
├── SKILL.md                    # Skill 定义
├── scripts/
│   ├── bp_client.py           # API 客户端
│   └── commands.py            # 命令实现
├── references/
│   ├── api-endpoints.md       # API 端点参考
│   └── kangzhe-rules.md       # 康哲规则参考
├── design/
│   └── design.md              # 本文档
└── setup.md                   # 安装说明
```

### 依赖
- Python 3.8+
- requests（用于 HTTP 请求）
- 环境变量：BP_APP_KEY（必需）

### API 调用流程
```
1. 获取周期列表 → 选择当前周期
2. 获取分组树 → 定位目标分组
3. 获取 BP Markdown 或调用管理接口
4. 格式化输出或执行操作
```

---

## 错误处理

### 常见错误
1. **缺少 BP_APP_KEY**：提示用户设置环境变量
2. **找不到分组**：提示用户检查姓名或权限
3. **无访问权限**：提示用户联系管理员
4. **API 调用失败**：记录错误并提示用户稍后重试

### 错误响应格式
```json
{
  "success": false,
  "error": "错误描述",
  "details": "详细信息（可选）"
}
```

---

## 扩展计划

### Phase 1（当前）
- ✅ BP 查看
- ✅ 新增关键成果/关键举措
- ✅ AI 检查（基础版）
- ✅ 延期提醒
- ✅ 汇报查看
- ✅ 搜索功能

### Phase 2（未来）
- ⏳ 编辑功能（需要 API 支持）
- ⏳ 删除功能（需要 API 支持）
- ⏳ AI 深度分析（基于 LLM）
- ⏳ 批量操作
- ⏳ 定时检查和提醒

---

## 性能考虑

1. **Markdown 接口**：Token 消耗较大，建议用于 AI 分析场景
2. **列表接口**：轻量级，适合快速预览
3. **详情接口**：按需获取，避免一次性加载过多数据
4. **批量接口**：用于批量查询，减少请求次数

---

## 安全考虑

1. **API Key 保护**：不硬编码，通过环境变量传递
2. **权限控制**：依赖后端权限校验，前端不绕过
3. **敏感信息**：不在日志中输出完整的 BP 内容
4. **错误信息**：不暴露系统内部细节

---

## 测试计划

### 单元测试
- API 客户端各接口的调用
- 命令参数解析
- 错误处理

### 集成测试
- 完整的用户场景流程
- 多步骤操作的原子性

### 手动测试
- 各种自然语言输入的识别
- 错误场景的用户体验

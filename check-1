#!/usr/bin/env python3
import json

vault_path = "/Users/evan/Library/Mobile Documents/iCloud~md~obsidian/Documents/日常学习"
with open(f"{vault_path}/.kb-workdir/kb_cache.json") as f:
    cache = json.load(f)

failed = sum(1 for v in cache.values() if v.get('status') == 'failed')
print(failed)

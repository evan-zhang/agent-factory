# 2026-04-02 Session Notes

## Methodology Validation #1 — Sub-Agent Spawn Rules Finalized

Evan confirmed three key decisions about sub-agent spawn methodology:

### Decision 1: Task Parameter Simplification
- Each sub-agent `task` parameter ≤500 chars
- Only includes: goal + input file paths + output format
- Detailed context passed via workspace files (not in task string)
- Must specify `cwd` when spawning sub-agents

### Decision 2: Failure Fallback Rules
- 1st failure → retry once (simplified task, verify `cwd` is correct)
- 2nd failure → Orchestrator degrades and executes the task itself
- Must log degradation to methodology validation log and inform user
- Rationale: reliability over purity; user experience matters more than architecture cleanliness

### Decision 3: S8 Continuous Maintenance — Flexible, No Sub-Stages
- S8 stays as one flexible stage, NOT subdivided into S8a/S8b/S8c/S8d
- Orchestrator handles flexibly: spawn sub-agents as needed (diagnose→fix→verify→publish)
- Reason: real-world maintenance work varies too much to standardize sub-steps
- Evan's exact words: "keep it flexible" rather than over-engineering

### Committed
- Commit `5da46a5` on `master`: All three decisions written into `AGENTS.md`

## Key Context Carried Forward
- **Sub-agent Analyst previously failed** during issue #9 diagnosis (output garbage, listed JS files)
- Root cause of failure: task too long, no `cwd` specified, scope too broad
- These three decisions are the direct fix for that failure pattern
- Next: apply improved spawn rules to retry issue #9 diagnosis

## Afternoon Session (15:30-17:45)

### Issue #9 Fix Completed
- Root cause: all 4 gateways had `hooks.internal` = null in openclaw.json
- Fix: wrote hook config to all gateways + `launchctl kickstart` restart
- Key learning: `openclaw gateway restart` uses generic plist (no .env), must use `launchctl kickstart gui/501/ai.openclaw.gateway.life`
- Killed 3 zombie `git add` processes consuming 99% CPU for days (RT-024 workspace)
- Closed #9

### Methodology Validation #1 — Guardrails Written to AGENTS.md
- Commit `b979809`: Sub-agent operation whitelist, Orchestrator execution discipline, gateway restart memo
- Commit `bca8ff6`: Refined network rule (allow web access, block config changes)
- 4 rules: sub-agent ops whitelist, mandatory verify after fix, env health check before diagnosis, gateway restart method

### Issues #10/#11 Fixed via Codex (gpt-5.2-codex)
- #10: Added `detect_combine_state()` to pull_core.py — uses chatListByPage API's combineState field instead of STT status
- #11: Resolved by correct meeting-end detection (no more false stops + unauthorized questions)
- Commit `1563736`, both issues closed
- Codex note: o3 model not supported on ChatGPT account, fell back to gpt-5.2-codex (worked well)

### Remaining Open Issues
- #8: CWork Skill 产品设计问题反馈 (feature request, low priority)

### CAS Daily Growth Log Generated
- Saved to `~/.openclaw/chat-archive/life/daily/2026-04-02.md`

## Afternoon Session (15:30-17:45)

### Issue #9 Fix Completed
- Root cause: all 4 gateways had `hooks.internal` = null in openclaw.json
- Fixed: wrote hook config to all gateways + `launchctl kickstart` restart
- Commits: b979809 (guardrails), bca8ff6 (network rule), 1563736 (#10/#11 fix)
- CAS hook verification: life confirmed writing to `2026-04-02.md` (5KB), ops/company/code need messages to trigger

### Issues #10/#11 Fixed (Codex gpt-5.2-codex)
- Root cause: `detect_runtime_status()` used STT status instead of `combineState`
- Fix: added `detect_combine_state()` using `chatListByPage` API
- Both issues closed

### Methodology Validation #1 Completed
- 4 rules committed to AGENTS.md: task≤500字, fallback, S8 flexible, operation guardrails, verify discipline
- Codex (gpt-5.2-codex) used for first time for code fix
- Analyst sub-agent timed out twice, Orchestrator degraded successfully
- Killed 3 zombie `git add` processes (99% CPU for days)

### Skill Installation Architecture Discussion
- Discussed global vs gateway vs workspace vs agent level skill installation
- Confirmed global installation as the strategy for all skills

## Open Items
- Issue #8 (cms-cwork feature request) — not started
- Skill publishing flow verification: publish to ClawHub, simulate user install
- Methodology validation #2: use next issue to validate guardrails
- CAS hook not configured on factory gateway

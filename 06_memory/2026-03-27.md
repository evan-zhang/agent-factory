## 2026-03-27 07:10 - AF-20260326-002 v1.0.3 hardening+optimization
- Evan 确认“继续”，执行了 CAS 的 P0+优化修复。
- 关键修复：
  1) `session-state` 并发冲突修复（唯一 tmp + state lock）
  2) hook 附件白名单（默认仅 gateway uploads，可配置扩展根）
  3) `record-bundle` 新命令，hook 改为单次子进程处理整轮消息
  4) 磁盘阈值控制（warn/min）
- 回归结果：`test_cas.py` 11/11 通过；`publish.py --dry-run --version 1.0.3` 通过。
- 新审计文档：`AUDIT-REPORT-v6-after-v1.0.3.md`。
- 当前结论：可继续灰度（life/factory/ops），非阻塞优化项为 hook 进程内直调与告警通道接入。

## 2026-03-27 07:28 - 需求一致性复核（无感知+免手工）
- 按 Evan 新要求做了“代码+需求”全量对照复核。
- 代码侧已强化：
  - hook 默认 fail-soft（阻断越界附件但不影响主流程）
  - 附件白名单默认包含 `uploads` 与 `state/media/{inbound,outbound}`
  - 新增 `record-bundle` 单次子进程归档整轮消息
  - 新增磁盘阈值控制（warn/min）
- 回归通过：11/11。
- 关键缺口确认：目标 gateway 的 `openclaw.json` 尚未挂载 hooks，导致“免手工自动归档”未完全达标。
- 结论：代码已可灰度；完成 hook 挂载后才能完全满足“用户无感+无需手工操作”。

## 2026-03-27 10:41 - 工厂流程增强（Skill设计档案+手动复盘）
- Evan 新需求：每个 skill 必须有完整设计方案与讨论记录；每次讨论后持续维护文档；并建立可提醒机制。
- 已落地：
  - SOP 升级至 `02_guides/AF-SOP-01...` v4.4（新增 Skill 设计档案强制项）。
  - 新增模板：`02_guides/AF-STD-02_Skill设计档案与学习复盘模板.md`。
  - cas-chat-archive 新增 `design/` 档案集（DESIGN/DISCUSSION-LOG/LEARNING-LOOP/SHARE-LOG）。
- 复盘策略：采用“手动触发复盘 + 去重分享（除非强制分享）”。
- 提醒机制：新增三条 cron（每日设计归档提醒、每周复盘分享提醒、每月复盘分享提醒）。
## 2026-03-27 11:22 (pre-compaction flush)
- Evan要求CAS项目从“仅归档”升级为“稳定归档 + 可运营查询 + 学习复盘”，并坚持“先全面复核再修关键风险”。
- AF-20260326-002 当前主线状态：v1.1.0-test 已开发完成，进入 S7 灰度验收（TESTING）。
- 本轮已落地能力：
  - cas_archive.py 支持 `--scope-mode gateway|agent` 与 `--agent`（按agent隔离归档能力）。
  - cas-chat-archive-auto hook 支持从 sessionKey 提取 agent 并透传；支持 `CAS_SCOPE_MODE`。
  - cas_inspect.py 支持 gateway/agent 双视角 report/search。
  - 新增 cas_review.py（daily/weekly/monthly/share-status/mark-shared）支持“手动复盘 + 去重分享 + 可强制重发”。
- 用户明确偏好：群协作先走“手动触发”而非全自动群播；默认不重复分享，已分享跳过，除非用户强制分享指定日/周经验。
- 工厂流程新要求（已确认）：每个Skill必须维护 design 档案（DESIGN/DISCUSSION-LOG/LEARNING-LOOP/SHARE-LOG），每次讨论后持续更新。
- 当前剩余关键缺口：四网关真实流量E2E验收仍需收口（重点 ops/company/code）；通过后再评估默认切换 `CAS_SCOPE_MODE=agent`。

## 2026-03-27 13:48 (internal market verification preference)
- Evan明确要求记住公司内部Skill市场主页：`https://skills.mediportal.com.cn/`。
- 新增固定执行规则：每次内部发布成功后，必须主动引导Evan到该主页做页面核验（确认skill可见与信息一致）。
- 该规则已并入工厂SOP（v4.6）与项目发布SOP（新增发布后页面核验步骤）。

## 2026-03-27 13:58 (new project kickoff)
- Evan要求基于“全新的Skill设计规格说明书”新建项目并启动。
- 已创建并启动项目：`AF-20260327-001` / `P20260327-001`。
- 当前状态：`DISCOVERY / WAITING_SPEC`，已初始化IDX、state.json、intake与discovery骨架。
- 阻塞：尚未收到可解析的规格书正文/附件路径；收到后将立即输出S1结构化理解并推进S2。

## 2026-03-27 14:18 (AF-20260327-001 progressed)
- 项目 AF-20260327-001（bp-reporting-templates复刻）已从 S2 推进到 S3。
- 用户“请继续”被记录为默认决策确认：API主+回退、仅Markdown、重试2次后人工确认、按请求子集并行。
- 已产出：`02_planning/REQ-01_需求确认草案.md`（确认版）、`03_battle/DESIGN-01_蓝图设计.md`、`04_execution/EXEC-PLAN-01.md`。
- 当前状态：S4代码实现已启动（R004=RUNNING）。

## 2026-03-27 15:08 (interaction gate requirement)
- Evan确认：bp-reporting-templates 生成前必须先做“双选择门禁”：
  1) 先列BP周期让用户选；
  2) 再列月/季/半年/年模板类型让用户选。
- 已在实现中落地：`scripts/main.py` 增加 `--list-periods` / `--list-template-types` 与未选择阻断机制。

## 2026-03-27 15:28 (period API verification)
- 在开发者平台 OpenAPI 文档中确认存在周期列表接口：`GET /bp/period/getAllPeriod`。
- 实测：spec key 可返回8个周期；`COMPANY_APP_KEY` 当前返回“Token校验失败”。
- 已在 bp-reporting-templates 实现中将周期列表查询优先切到该接口，并新增“企业appKey权限开通”跟进项。

## 2026-03-27 16:02 (cwork dispatch + API validation closure)
- AF-20260327-001 已确认并实测开发者平台存在 BP 周期接口：`GET /open-api/bp/period/getAllPeriod`。
- 差异结论：spec key 可正常返回周期列表；`COMPANY_APP_KEY` 当前返回 `Token校验失败`，问题归因到企业侧鉴权/权限开通，而非接口缺失。
- 代码侧已对齐：`bp-reporting-templates/scripts/api_client.py` 的 `list_periods()` 优先走 `/bp/period/getAllPeriod`，并将 `status==1` 识别为当前可用周期。
- 按 Evan 指令“用 cwork 发送”，已通过 CWork 接口发送工作汇报给成伟：
  - 接口：`POST /open-api/work-report/report/record/submit`
  - 接收人 empId：`1514822118611259394`（成伟）
  - 发送回执：`resultCode=1`，`id=2037434574635814913`

## 2026-03-27 16:07 (standard app key policy)
- Evan确认：后续开发所有 Skill 统一使用同一套已验证可用的标准 appKey（本轮已验证的 key）。
- 执行口径：仅通过环境变量注入（优先 `BP_APP_KEY`），代码中禁止硬编码 key。

## 2026-03-27 16:24 (bp-reporting-templates continued development)
- 在 Evan “继续开发”授权下，AF-20260327-001 完成第二轮 S4 代码推进：
  - `api_client.py` 新增 `resolve_org_name()`（基于 `/bp/group/searchByName`）实现输入组织自动识别；
  - `fetch_bp_data()` 改为递归抽取目标节点，并在 `measureStandard` 缺失时回退关键成果字段；
  - 修复 `template_manager.py` 模板目录路径错误；
  - `main.py` 增加 dataclass/dict 兼容转换与 `--org-name/--person-name` 覆盖参数；
  - 审查未通过时也保留产物（`DRAFT + REVIEW.json`），避免结果丢失。
- 实链路验证通过：
  - 命令：`python main.py '为集团生成季报' --period-id 1994002024299085826 --template-types 季报 --app-key <standard-key>`
  - 结果：自动识别组织“集团”，成功产出 `集团_组织_季报填写规范.md`。

## 2026-03-27 16:46 (continue development round)
- Evan下达“继续”后，AF-20260327-001 继续推进到 v0.3.3-dev-reviewer：
  - reviewer 收紧（编码缺失升级 error、零容忍项检测、偏离判断占位符检测）；
  - parser 增加 JSON 文件回退解析；
  - 单测扩展至 7/7 通过；
  - API 与文件回退两条 E2E 均可执行，审查未通过时输出 DRAFT + REVIEW 报告。
- 下一开发焦点：filler 章节补全 + reviewer 红黄绿阈值自动判定，目标进入 S5 质检。

## 2026-03-27 16:56 (quarterly pass achieved)
- 在 Evan 再次“继续”后，AF-20260327-001 完成 filler 收口与 reviewer 降噪：
  - 移除个人/组织编码占位符，默认映射到目标编码；
  - 偏离判断默认实填（绿/否/0%）；
  - 通用占位符（[填写:...]/[待填写:...]/[描述]/[是/否]）批量收敛；
  - 零容忍检测改为风险模式匹配，减少误报。
- 当前结果：季报链路已稳定过审（API链路 + JSON文件回退链路均输出正式稿）。
- 项目版本更新：`0.3.4-dev-pass-quarterly`，下一步推进月报/半年报/年报一致化过审。

## 2026-03-27 17:19 (4-set regression passed)
- Evan再次“继续”后，AF-20260327-001 完成四模板一致化回归（API实链路）：
  - 命令：`main.py '为集团生成四套' --period-id 1994002024299085826 --template-types 四套 --app-key <standard-key>`
  - 结果：月报/季报/半年报/年报 4/4 全部审查通过并输出正式稿。
- 项目版本升级到 `0.3.5-dev-pass-4set`，当前进入 S5 质检门控材料准备阶段。

## 2026-03-27 17:50 (S5 gate started with user approval)
- Evan在“同意”后，AF-20260327-001 已正式切换到 S5 TESTING。
- 已输出门控文档：`05_closure/TEST-01_S5测试门控报告-20260327.md`。
- 现状态：`version=0.4.0-s5-gate`，S5 收口项为阈值自动判定与误报对照补测，完成后进入 S6 发布准备。

## 2026-03-27 20:00 (S5 threshold checks completed)
- Evan“继续”后，AF-20260327-001 完成 S5 关键收口：
  - reviewer 接入阈值自动判定（从 `alert_rules.yaml` 读取阈值），新增“颜色阈值不一致”检查；
  - 增加误报对照测试与阈值不一致测试；
  - 单测提升到 9/9 通过；
  - API 四套回归复测继续 4/4 通过。
- 项目版本更新为 `0.4.1-s5-threshold`，进入 S5 最后收口并准备 S6。

## 2026-03-27 20:09 (S5 final closure and S6 prep ready)
- Evan“继续”后，AF-20260327-001 已完成 S5 最终收口：新增 `TEST-02_S5最终收口报告-20260327.md`。
- 同步完成 `RELEASE-01_S6发布准备清单-20260327.md`，明确双通道发布步骤与阻断条件。
- 状态更新为 `READY_FOR_RELEASE`，版本 `0.4.2-s6-prep`；当前待执行 S6（ClawHub + 内部市场）。

## 2026-03-27 20:40 (CWork UAT report sent with attachments)
- 按 Evan 要求已生成《CWork工作协同汇报（人工测试）》并打包9部门月报/季报共18份产物（ZIP）。
- 已通过 CWork 汇报接口发送给侯桐（empId: 1514822114043662337），携带2个附件：汇报MD + 产物ZIP。
- 发送回执：`resultCode=1`，汇报ID=`2037510232166490113`。
- Evan 本人 CWork 接收ID尚未确认，待补充后可一键补发同内容。

## 2026-03-27 21:11 (critical messaging preference correction)
- Evan确认中文名为“张成鹏”（evan 为英文名）。
- Evan明确要求：任何外发（尤其CWork汇报）必须先经其逐次确认与同意，未确认不得发送；此前存在误发/附件不全问题，后续必须避免。

## 2026-03-27 21:27 (pre-send confirmation protocol)
- 张成鹏反馈：后续任何外发前，必须先给其展示并确认关键信息：汇报标题、正文、接收人、抄送人、建议人、审批人等；确认后才可发送。
- 本次认可点：附件发送正确（三附件口径正确）。
- 执行策略：发送前固定走“发送前确认单”流程，未经“确认发送”不得外发。

## 2026-03-27 21:40 (send-after-confirm executed)
- 在 Evan 明确“确认”后，按确认单发送 CWork 汇报：
  - 标题：玄关健康BP 月度及季度各部门填报模板
  - 收件人：张成鹏、侯桐
  - 抄送：屈军利、李广智、成伟
  - 附件：UAT清单 + UAT汇报 + UAT总打包（3个）
- 发送成功：汇报ID=2037525218972524545。

## 2026-03-27 21:45 (release execution blockers)
- AF-20260327-001 已具备发布条件（READY_FOR_RELEASE），但执行S6时遇到两项外部阻断：
  1) ClawHub publish 在 `Preparing bp-reporting-templates@0.4.2` 阶段连续超时；
  2) 内部市场发布脚本 `publish_skill.py` 依赖 `XG_USER_TOKEN`，当前环境未提供。
- 已记录到 `05_closure/RELEASE-ATTEMPT-LOG-20260327.md`。

## 2026-03-27 21:53 (default app key persistence)
- Evan要求将其内部市场/开放平台 key 设为默认环境变量，后续无需重复索取。
- 已持久化到 `~/.zshrc` 与 `~/.zprofile`：`BP_APP_KEY` / `COMPANY_APP_KEY`。
- 同步执行 `launchctl setenv`，用于当前用户会话下新进程继承。

## 2026-03-27 22:08 (SeaWork/CWork report flow hardening)
- 按 Evan 指示已开始改造 CWork 工作汇报流程（先确认后发送）。
- 新增脚本：`05_products/create-xgjk-skill/scripts/work-report/send_report_with_confirm.py`
  - `prepare`：生成发送前确认单（标题/正文/To/CC/建议人/审批人/附件、姓名解析结果、readyToSend）
  - `send`：仅在 `--confirm-token CONFIRM_SEND` 下执行发送
  - 附件采用已验证稳定方案：`uploadWholeFile` 后 `fileVOList` 仅传 `fileId+name+type`
  - 姓名解析加入常见错别字纠正（如 屈俊丽→屈军利、陈伟→成伟候选提示）
- 已将该流程和约束写入 `05_products/create-xgjk-skill/SKILL.md`（新增“工作汇报发送”能力与强制规则）。

## 2026-03-27 22:26 (SeaWork/CWork API capability check + skill expansion direction)
- Evan requested verification whether CWorker/CWork developer platform APIs have expanded and whether new capabilities can be added to SeaWork skill.
- Confirmed OpenAPI groups available at `/open-api/swagger-resources`:
  - `03-work-report` (22 APIs)
  - `04-cwork-file` (2 APIs)
  - `05-cwork-user` (6 APIs)
- Existing stable sending path remains valid:
  - `cwork-user/searchEmpByName` → `cwork-file/uploadWholeFile` → `work-report/report/record/submit`.
- Additional APIs identified as high-value for near-term expansion:
  - Inbox/outbox query, unread/new message list, read-report acknowledge, report reply, template list, todo completion.
- Product decision direction proposed:
  - P0 (must): enforce pre-send confirmation gate + name resolution + attachment stable flow + explicit confirm-send token.
  - P1 (next): post-send outbox verification + unread dashboard + reply capability.
  - P2 (later): template-driven send + todo closed-loop + AI Q&A stream.
- Implemented groundwork in `create-xgjk-skill`:
  - Added `scripts/work-report/send_report_with_confirm.py` (prepare/send two-phase flow).
  - Added `scripts/work-report/README.md`.
  - Updated `create-xgjk-skill/SKILL.md` with work-report routing/capability and mandatory constraints.

## 2026-03-27 22:37 (CWork request sent after explicit confirmation)
- Evan confirmed send for urgent API request to work-collaboration side about draft-box APIs.
- Sent via CWork to 成伟（empId 1514822118611259394）with request scope: draft create/read/update/delete/cleanup + state transition/auth/attachment/idempotency/error codes.
- Advisory/decision context included in body: 建议人=成伟，决策人=侯桐、屈军利。
- Send receipt: `resultCode=1`, report id=`2037539429777793026`.

## 2026-03-27 22:40 (fix: decision/advisor not bound in submit payload)
- Evan reported sent report did not correctly set decision makers.
- Root cause confirmed: earlier `send_report_with_confirm.py` only displayed advisors/approvers in preview text and did not submit them in API payload.
- Fix implemented:
  - `prepare` now resolves advisors/approvers to employee IDs (same resolver as To/CC), records unresolved candidates by scope.
  - Builds `reportLevelList` with:
    - level=1, type=suggest, levelUserList=advisors
    - level=2, type=decide, levelUserList=approvers
  - `send` now includes `reportLevelList` in `/work-report/report/record/submit` payload.
- Validation:
  - compile passed;
  - prepare test with 成伟/侯桐/屈军利 produced correct advisorEmpIdList, approverEmpIdList and reportLevelList.

## 2026-03-27 22:43 (resend with corrected suggest/decide binding)
- Evan approved resend after fixing API payload for advisor/decider.
- Resent request to 成伟 with corrected `reportLevelList` binding:
  - suggest: 成伟(1514822118611259394)
  - decide: 侯桐(1514822114043662337), 屈军利(1514822127830339585)
- Send receipt: `resultCode=1`, report id=`2037541237753667585`.

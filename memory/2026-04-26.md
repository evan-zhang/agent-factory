# Memory 2026-04-26

## 七大政策采集 Skill Pack — 完整迭代记录

### 项目概况
- 项目编号：2604233
- 标准名称：`seven-policy-collector`（Pack 总控入口 SKILL.md 已创建）
- 当前版本：v1.2.2
- 7 个子模块，每个输出 3 个 Markdown（指标汇总表/来源明细表/缺口与待补充）
- 依赖：`multi-search`（项目 2604251，通用搜索降级工具包）

### 版本迭代链（v1.0.0 → v1.2.2）
- v1.0.0：初始版本，Issue #30 测试发现 2P0+3P1+3P2
- v1.0.1：修复 Tavily 残留（去品牌化+双通道声明）、setup --key 参数、OpenClaw 残留、占位符、验证逻辑误判
- v1.0.2：修复 --key 被 L68 覆盖（BUG-6）、API Host 硬编码国际版（BUG-7）
- v1.1.0：EXECUTION-PROMPT 重构（Phase 0 拆成初始化+能力探测）+ 7文件去品牌化
- v1.1.1：7 个检索词模板从单轮 site: 改为三轮递进（精准→泛搜→兜底）
- v1.1.2：setup 脚本新增 Step 6 环境能力探测 + Exa 配置指南
- v1.1.3：搜索降级链加入 Tavily（MiniMax→Tavily→Exa→web_fetch）
- v1.1.4：修复 BUG-8（配置指南 OpenClaw 残留）、BUG-9（EXECUTION-PROMPT tavily 残留）、DESIGN-2（03 表头加缺口原因列）
- v1.2.0：OpenClaw + Hermes 双运行时兼容改造（setup 脚本自动检测运行时）
- v1.2.1：修复 BUG-10（Hermes 验证分支 VERIFY_OK 被覆盖）
- v1.2.2：彻底分离 OpenClaw/Hermes 验证逻辑

### Issue 关联
- #30（v1.0.0 的 5 个 bug）✅ 已关闭
- #31（v1.0.1 的 2 个 P0）✅ 已关闭
- #32（v1.1.0 功能增强：搜索抓取能力）✅ 已实现，审核后改为声明式方案
- #33（v1.1.0 的 4 个问题）✅ 已关闭
- #34（v1.2.0 Hermes 验证 bug）✅ 已关闭

### multi-search Skill（项目 2604251）
- 从七大政策项目中提取的通用搜索基础设施层
- v1.0.0，独立 Skill
- 包含：搜索降级链、抓取降级链、三轮递进检索策略、双运行时适配
- 不含业务逻辑（检索词模板由调用方定义）

### 关键设计决策
1. **不自建 fetch_utils.py**（违反核心红线，等 OpenClaw 内置 Playwright）
2. **references 只声明需求**（不写降级链实现，去品牌化）
3. **搜索降级链**：MiniMax → Tavily → Exa → web_fetch → 停止
4. **双运行时适配**：setup 脚本自动检测 OpenClaw(mcporter) / Hermes(config.yaml)
5. **Hermes 的 browser 工具**支持 JS 渲染（bendibao 实测 57 元素完整抓取）

### 远程 Claude Code（SSH 方式）
- Mac Studio 192.168.91.72，用户 xgstudio，已配 sshpass
- 用途：本机内存不足时通过 SSH 调远程 Claude Code 审核方案
- 命令模式：`sshpass -p 'xg@123' ssh -o StrictHostKeyChecking=no xgstudio@192.168.91.72 'export PATH=/opt/homebrew/bin:$PATH; claude --print --dangerously-skip-permissions --max-turns N "prompt"'`
- 注意：prompt 长度有限制，复杂任务应先把文件 scp 过去再让 Claude 读

### 待办：Cloud File Store 集成
- Evan 提供了 Cloud File Store API 规范（HTTP 文件存储服务，默认端口 8900）
- 目标：把采集的政策数据有序存到云端
- 已提出存储路径设计和集成方案，等待 Evan 确认 4 个决策点：
  1. 按日期版本化 vs 只保留最新
  2. metadata.json 字段设计
  3. 逐模块上传 vs 整批上传
  4. 配置传入方式（环境变量 vs setup 参数）
- 新增文件：scripts/sync-to-cloud.sh + docs/cloud-storage-guide.md

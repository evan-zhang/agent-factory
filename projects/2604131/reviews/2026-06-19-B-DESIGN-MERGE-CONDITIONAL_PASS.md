## 评审结论

**总体评级**：CONDITIONAL_PASS

**评审对象**：B 类 Agent / Skill 规范文档 — `projects/2604131/link-archivist/docs/MERGE_PLAN_v2.md`（v1.0 → v2.0 修复版）
**评审时间**：2026-06-19

---

**复评逐项判定**

| # | v1.0 必修项 | 判定 | 一句话说明 |
|---|---|---|---|
| 1 | 5 个未决问题是否收敛为可执行设计决策 | PASS | §13.1-§13.5 已把 deprecated 周期、LLM 重编、多目录、语义搜索、Phase 3 模板逐项改为明确决策。 |
| 2 | 配置迁移策略是否消除了“删除 vs 保留”的矛盾 | NOT_FIXED | §7.3 明确“备份而非删除”，但 §4.5 仍写 case A 合并后“删除旧文件”，同一方案内仍存在矛盾。 |
| 3 | 多文件索引写入是否有原子写/锁/回滚/重建策略 | PASS | §3.2 已明确 fcntl 锁、tmp + os.replace 原子写、dirty 标记、基于 entries.json 的自愈重建策略。 |
| 4 | 三层披露边界是否明确 | PASS | §5.5 已划清 SKILL.md、references、设计文档/代码 docstring 的承载边界及 Agent 读取顺序。 |
| 5 | Phase 3 frontmatter 字段是否有稳定输出模板 + 校验规则 + 用户反馈格式 | PASS | §13.5 已给出标准 prompt 模板、frontmatter_invalid 校验规则和索引失败时用户可见反馈格式。 |

---

**维度评分**

| 维度 | 评分（1-5） | 简评 |
|---|---:|---|
| 设计与需求的一致性 | 4 | v2.0 基本回应 v1.0 的核心风险，合并目标、归档即索引和查询能力均可执行；仅配置迁移仍有残留矛盾。 |
| 唯一真相源 | 4 | 方案把 archive_dir 设为唯一目录源，并明确 entries.json 为索引事实源；但 §4.5 与 §7.3 的迁移表述仍需统一。 |
| 三层披露合理性 | 4 | 披露层级已明确，references 与设计文档分工清楚；但 SKILL.md 预计 400-450 行偏重，后续实现时仍应压缩。 |
| 触发词设计 | 4 | 查询、重建、增量、巡检等触发场景覆盖口语化表达，默认 keyword 与 semantic 降级规则也清晰。 |
| 失败兜底与错误处理 | 4 | 索引失败、并发写、dirty 自愈、frontmatter_invalid 反馈均有兜底；配置迁移文案冲突会影响实现一致性。 |

---

**关键问题**（最多 5 个）

1. [严重度：中] 配置迁移策略仍未完全消除“删除 vs 保留”的矛盾：§4.5 写“一致则合并后删除旧文件”，§7.3 写“重命名为 .bak，永久保留”。 → 修复建议：将 §4.5 的“删除旧文件”改为“重命名为 `kb-graph-config.json.bak`，系统不主动删除”，并引用 §7.3 为准。
2. [严重度：低] §10 风险表仍保留“强制以 archive_dir 为准”的旧表述，和 §7.4 的迁移窗口/用户选择口径不完全一致。 → 修复建议：把风险缓解改为“默认以 archive_dir 为准；冲突时保留旧配置并提示用户选择”。
3. [严重度：低] §5.5 预计 SKILL.md 400-450 行，虽边界已清楚，但仍偏离“启动核心文件精炼”的 Token 经济目标。 → 修复建议：实施时把详细表格、命令说明和长模板全部下沉 references，SKILL.md 只保留触发、主流程和失败反馈摘要。

---

**最重要的一条建议**

先统一配置迁移的唯一口径：旧 `kb-graph-config.json` 只能备份为 `.bak` 并保留，方案全文不要再出现“删除旧文件”。

## 评审结论

**总体评级**：CONDITIONAL_PASS

**评审对象**：A 类（业务设计方案） — bd-eval-cms v0.10.0 内化搜索能力方案（修订版 v2）
**评审时间**：2026-06-14

---

**维度评分**

| 维度 | 评分（1-5） | 简评 |
|------|-----------|------|
| 需求完整性 | 4 | 5 个脚本职责清晰，"不做什么"列表完善，但配额管理实现细节缺失 |
| 可行性 | 3 | 拆分架构合理，但 5 个脚本行数估算（2-3K）过于乐观，协调成本被低估 |
| 价值合理性 | 4 | 内化决策方向对，但"轻量化"价值需权衡：失去 MCP 抽象层 vs 减少依赖 |
| 风险识别 | 3 | 列出 5 项风险，但漏了关键风险：半自动字段抽取的 UX 防护、关键词模板的升级路径 |
| 边界明确性 | 5 | 8 条"不做"清晰，包括 v0.10.0/v0.10.1 分期边界、源分级边界 |

---

**关键问题**（最多 5 个）

1. **[严重度：高] "60 秒窗口配额计数"实现细节缺失** → 修复建议：必须在方案中补充：（1）数据结构（JSON 文件格式示例），（2）持久化路径（`scripts/search/lib/quota.json` 文件是否跨进程共享），（3）并发控制（如何防止多个 sub-agent 同时调用时的竞态条件），（4）窗口重置逻辑（60 秒后如何检测并重置计数器）。当前方案只说"用配额管理"，未说明技术路径。

2. **[严重度：高] search/ 子目录代码量 2-3K 行估算过于乐观** → 修复建议：实测验证每个脚本的复杂度。core_search.sh 需覆盖 web_search 调用、轮询、抓取、重试、配额拦截、降级逻辑（web_search → web_fetch），100 行可能不够。field_extractor.sh 需覆盖正则抽取 + LLM 提示词生成 + 人工确认流程，120 行可能不够。建议先写伪代码测行数，再承诺。

3. **[严重度：中] 半自动字段抽取缺乏 UX 防护机制** → 修复建议：v0.10.0 半自动流程（field_extractor.sh 输出提示词模板 → sub-agent 用 LLM 抽取 → 人工确认）存在 sub-agent"偷懒"风险：sub-agent 可能不调用提示词模板，直接用 LLM 知识答问题，绕过证据收集。必须在 orchestrator 调用前加校验：检查 sub-agent 输出中是否含至少 1 个 `[P{prefix}-XXX]` 引用，否则标记 failed。

4. **[严重度：中] 5 个独立脚本维护成本 vs 复杂度权衡未充分论证** → 修复建议：方案称拆分后多 2h（接口协调），但未论证"5 个小脚本"vs"1 个大脚本"的长期维护成本。拆分后的代价是：修改一个功能可能需要协调多个脚本（如降级逻辑改了要同步 core_search.sh + validate_gate_search.sh），而大脚本虽然复杂，但修改集中。建议在"风险"节补充"维护成本 vs 复杂度"的分析。

5. **[严重度：低] 关键词模板升级路径缺失** → 修复建议：22 个 skill 加搜索规范后，如果某个 skill 的关键词模板过时（如 FDA 改了 site:fda.gov 的 URL 规则），升级路径不明确。是否需要人工定期 review？还是加 automated detection（如统计 skill 的搜索失败率）？建议在"风险"节补充"模板过时检测机制"。

---

**最重要的一条建议**

**先写伪代码测行数，再承诺工作量。** 5 个脚本的行数上限（100/80/100/120/100）是基于职责拆分的理论值，未考虑实际复杂度。建议在实现前先为每个脚本写详细的伪代码（含函数签名、错误处理、边界逻辑），测实际行数。如果超过上限，要么调整职责边界，要么承认估算偏差 + 修正工作量到 20h。

---

## 第一轮 5 个问题修复情况

### 问题 1：search.sh 300 行拆分 ✅ 已修复
- 修复策略：拆分为 5 个独立脚本（core_search.sh / source_ranker.sh / keyword_mapper.sh / field_extractor.sh / validate_gate_search.sh）
- 行数上限：100/80/100/120/100（总计 500 行 vs 原 300 行）
- 职责边界：每个脚本的"不做什么"列表清晰
- **评价**：修复到位，拆分合理

### 问题 2：字段抽取自动化可行性 ✅ 已修复
- 修复策略：分两期 - v0.10.0 半自动（提示词模板 + 人工确认），v0.10.1 全自动（>85% 准确率验证后）
- v0.10.1 验证：跑 10 篇 FDA 文档 + 5 篇 NMPA 公告测准确率
- **评价**：修复到位，技术验证前置

### 问题 3：降级链裁剪判断 ⚠️ 部分修复（接受设计权衡）
- 修复策略：明确说明 multi-search 的 3 个 yaml 文件是 MCP/抽象层，OpenClaw 内置 web_search 不需要这些层
- 等价能力：配额管理（60 秒窗口计数）、超时控制（bash timeout）、降级链（core_search.sh if-else）
- **评价**：方案明确裁剪是"简化调用"的主动选择，不是"移除冗余"的误判。但配额管理实现细节缺失（见问题 1）

### 问题 4：22 个 skill 分布实测 ✅ 已修复
- 实测数据：search-heavy (4 个), search-medium (2 个), search-light (11 个), search-minimal (2 个)
- 分级策略：heavy/medium/light/minimal 加规范量（40/25/10/0 行）
- **评价**：修复到位，基于实测数据设计差异化策略

### 问题 5：orchestrator 校验插入点 ✅ 已修复
- 插入点：行 79 `mark_gate()` 函数之后
- 伪代码：validate_gate_search_evidence() 函数（校验 references/{gate_prefix}/ 目录新增文件数 + 引用数）
- **评价**：修复到位，插入点明确

---

## 修订方案新增内容评估

### 1. 5 个脚本的"不做什么"列表 ✅ 清晰
- 每个脚本都有明确的"不做什么"列表
- 无明显重叠
- **评价**：职责边界清晰

### 2. 源分级表 T1-T4 credibility 阈值 ✅ 合理
- T1 (0.95): 政府/监管（nmpa.gov.cn / fda.gov / ema.europa.eu）
- T2 (0.85): 临床/学术（clinicaltrials.gov / pubmed）
- T3 (0.7): 行业数据库（iqvia.com / 米内网）
- T4 (0.5): 公司方/媒体
- **评价**：阈值符合业务直觉

### 3. 关键词模板节选示例 ⚠️ 覆盖不完整
- 方案只列了 6 个 skill 的示例（A-1 / A-4 / A-5 / B-1 / D-1 / D-2）
- 缺少其他 16 个 skill 的示例
- **评价**：建议补充至少 search-heavy + search-medium 的所有示例

### 4. 配额管理"60 秒窗口计数"实现 ❌ 缺失
- 方案称"60 秒窗口计数"，但未说明：
  - 数据结构（JSON 文件格式？）
  - 持久化路径（quota.json 位置？）
  - 并发控制（多个 sub-agent 同时调用？）
  - 窗口重置逻辑（60 秒后如何检测？）
- **评价**：必须补充技术路径（见问题 1）

### 5. "16h 工作量" vs 原 14h ⚠️ 拆分代价未充分论证
- 原估算 14h → 修订 16h（+2h）
- 理由：拆分后多 2h（接口协调）
- **评价**：2h 是否够？5 个脚本需要统一参数格式（query / results）、错误处理标准、日志格式，协调成本可能不止 2h。建议补充"接口设计规范"（如统一输入/输出 JSON 格式）

---

## 上一轮没问到、这一轮要问的（回答）

### Q1: search/ 子目录总代码量预估？
- 方案承诺 2-3K 行
- **问题**：过于乐观，见问题 2。建议先写伪代码测行数。

### Q2: 5 个脚本的接口设计是否需要统一规范？
- 方案未提及
- **建议**：必须统一。建议在 scripts/search/lib/ 下加接口规范文档（如 `interface-spec.md`），定义：
  - 统一输入格式（JSON）
  - 统一输出格式（JSON）
  - 统一错误码（如 ERR_QUOTA_EXCEEDED / ERR_TIMEOUT / ERR_NO_RESULTS）
  - 统一日志格式（"[TIMESTAMP] [SCRIPT_NAME] [LEVEL] message"）

### Q3: "OpenClaw 未来提供 minimax 风格 MCP 时迁移"的监测机制？
- 方案称"监测 release notes"
- **建议**：具体化。建议在 scripts/search/lib/ 下加 `release-notes-monitor.sh`，每月执行 `git log` + `grep` 检测 OpenClaw 是否新增 MCP 接口。如果检测到，发送告警（如写入 `projects/2605281/bd-eval-cms/state.json` 的 `migration_needed` 字段）。

### Q4: v0.10.1 全自动字段抽取的触发条件（准确率 >85%）怎么测？
- 方案称"跑 10 篇 FDA 文档 + 5 篇 NMPA 公告测准确率"
- **建议**：具体化测试集。建议在 references/ 下加 `search-field-extraction-test-cases.md`，列出：
  - 10 篇 FDA 文档的 URL（如 https://www.accessdata.fda.gov/cder/daap/daapArchive/...）
  - 5 篇 NMPA 公告的 URL
  - 每个文档的"预期抽取字段"（如批准日期 / 适应症 / 持有人）
  - 人工标注的"真实答案"（作为 ground truth）

### Q5: health-check 改回"⚠️ 建议"后，是否还需要"已装检测"？
- health-check.sh v0.9.4.2 已取消 multi-search 硬依赖
- 方案称"health-check 恢复 multi-search 为 ⚠️ 建议"
- **建议**：不需要"已装检测"。因为 multi-search 不再是硬依赖，health-check 只需提示"建议安装 multi-search 作为人工增强工具"，无需检查是否已装。

---

## 风险评估修订（补充）

### 1. 5 个独立脚本 vs 1 个大脚本：维护成本 vs 复杂度
- **5 个小脚本代价**：
  - 修改一个功能可能需要协调多个脚本（如降级逻辑改了要同步 core_search.sh + validate_gate_search.sh）
  - 接口变动影响面广（如统一输入格式改了要同步 5 个脚本）
- **1 个大脚本代价**：
  - 单文件复杂度高（但可读性靠函数封装）
  - 单点故障（但测试成本低）
- **权衡建议**：在"风险"节补充"维护成本 vs 复杂度"分析，明确选择 5 个小脚本的依据（如"单一职责"原则）。

### 2. 半自动字段抽取 UX 防护：sub-agent 偷懒风险
- **风险**：sub-agent 可能不调用 field_extractor.sh 输出的提示词模板，直接用 LLM 知识答问题
- **缓解**：见问题 3。必须在 orchestrator 调用前加校验：检查 sub-agent 输出中是否含至少 1 个 `[P{prefix}-XXX]` 引用

### 3. 22 个 skill 批量加规范后，关键词模板过时升级路径
- **风险**：如果某个 skill 的关键词模板过时（如 FDA 改了 site:fda.gov 的 URL 规则），升级路径不明确
- **缓解**：见问题 5。建议在"风险"节补充"模板过时检测机制"（如统计 skill 的搜索失败率）

---

## 评审结论解释

**CONDITIONAL_PASS**（方向对，有必须修复的问题，修完不用重评即可放行）：

- **方向对**：内化搜索能力的决策方向正确，5 个脚本拆分解决了第一轮的"300 行单一脚本不可行"问题，字段抽取分两期解决了"自动化未验证"问题，降级链裁剪明确了"简化调用"的设计权衡。

- **必须修复**：
  1. "60 秒窗口配额计数"实现细节缺失（必须补充数据结构 / 持久化路径 / 并发控制 / 窗口重置逻辑）
  2. search/ 子目录代码量 2-3K 行估算过于乐观（必须先写伪代码测行数）
  3. 半自动字段抽取缺乏 UX 防护机制（必须加 sub-agent 输出引用校验）
  4. 5 个独立脚本维护成本 vs 复杂度权衡未充分论证（必须补充分析）
  5. 关键词模板升级路径缺失（必须补充"模板过时检测机制"）

- **修完不用重评**：以上 5 个问题修复后，方案的核心设计（内化 + 业务特定能力 + 5 脚本拆分）不变，无需重评。

---

## 与工厂经验对齐（EXPERIENCE.md）

参考工厂经验：
- [2026-05-30] **报告合并必须是程序行为，不能让 AI 做** → search/ 子目录的 5 个脚本必须明确程序行为（如 quota.json 的读写是 bash 层做，不是 AI 做），避免 AI 介入破坏可复现性。
- [2026-06-12] **引用文档中的"有效期/限额"类信息必须独立验证** → multi-search 的配额管理（quota_limit: 100/60/50）在 v2 中移除了，但新的"60 秒窗口计数"必须验证技术可行性（不能假设可行）。

---

## 评审者立场

我评审的是**修订方案**，不是原方案。修订方案对第一轮 5 个问题的修复是到位的：
- 300 行拆分 → 5 个小脚本 ✅
- 字段抽取自动化 → 分两期 ✅
- 降级链裁剪 → 明确设计权衡 ✅
- 22 个 skill 分布 → 实测数据 ✅
- orchestrator 校验插入点 → 明确代码位置 ✅

但修订方案引入了**新问题**：
- 配额管理实现细节缺失
- 代码量估算过于乐观
- 半自动字段抽取 UX 防护缺失
- 维护成本 vs 复杂度权衡未论证
- 关键词模板升级路径缺失

这些新问题不是第一轮问题的遗漏，而是修订方案在修复过程中引入的。必须修复后才能放行。

---

评审完成。
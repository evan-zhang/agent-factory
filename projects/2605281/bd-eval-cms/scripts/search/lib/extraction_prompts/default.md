# 默认字段抽取提示词（兜底）

**用途**：未指定 Gate 类型时使用

**抽取原则**：
1. 严格基于抓取文本，禁止 AI 自由发挥
2. 找不到的字段填 `null`，禁止编造
3. 日期统一 YYYY-MM-DD
4. 数字字段必须为有效数字
5. 输出结构化 JSON 后**人工确认**关键字段

**流程**：
1. 读取 fetch 内容
2. 抽取候选字段值
3. 输出 JSON
4. 用户确认
5. 写入 references/{prefix}/{prefix}-XXX.md

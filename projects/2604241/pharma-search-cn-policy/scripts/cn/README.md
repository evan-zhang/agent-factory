# 脚本索引：`cn`

| 脚本 | 鉴权 | 用途 |
|------|------|------|
| `append_evidence.py` | `nologin` | 校验并追加一行到 `evidence.jsonl` |

## 示例

```bash
python3 scripts/cn/append_evidence.py \
  --run-root "/path/to/RUN_ROOT" \
  --evidence-json "./one-evidence.json"
```

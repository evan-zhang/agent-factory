# 04-购房资格

本文件是“购房资格”模块的内置模块文档与字段约定，用于帮助 skill 理解常见渠道结构、户籍与家庭情形区分、社保或个税条件和限购套数映射。

真实执行时，本文件就是默认模块文档。执行代理必须先读本文件，并按本文件的渠道模板、标签和指标映射进行检索。这里的占位符用于生成目标城市查询，不是已采集来源。

## 检索工具要求

- 搜索检索优先使用 `MiniMax web_search`（via mcporter call minimax.web_search）
- 如果 MiniMax 不可用，回退到 `web_fetch`（网页抓取）
- 两者都不可用时，停止执行并说明缺少检索技能
- 实际使用哪个通道，须在来源明细的备注中记录
- 使用本文件中的渠道模板、渠道顺序和检索词模板构造查询
- `目标城市` 和 `年份/时间范围` 必须来自用户输入；搜索查询、来源明细和缺口记录都必须保留该年份口径
- 搜索工具只用于发现和定位页面；采集内容必须来自已打开页面
- 搜索结果摘要、片段或未打开页面不能当作官方原文
- 候选 URL 必须实际打开并验证可读；404、错误页、空白页、登录拦截页或不可读页面不能作为来源证据
- 采用来源时，`02-来源明细表.md` 的 `备注` 必须记录 URL 验证结果，以及 搜索结果标题/摘要/排名

## 与 SKILL.md 的配合方式

- 本 reference 不单独执行，必须配合上级 `SKILL.md` 使用
- `SKILL.md` 决定固定指标、输出文件、来源字段和追溯规则；本文件提供内置渠道模板、模块标签、关键词和指标映射
- 执行时先读本文件，再根据目标城市替换占位符并使用搜索工具定位真实官方页面
- 如用户额外提供城市材料，只能作为补充线索；不得替代本文件的渠道顺序、固定指标和输出约束

## 检索与存储提醒

- 对每个指标至少尝试使用对应关键词进行检索；找到相关页面后必须打开页面再采集
- 采用的每个页面都要写入 `02-来源明细表.md`，并保留完整 URL、URL 验证结果、检索关键词、搜索结果标题/摘要/排名、原文摘录和提取数据
- 搜索结果 URL 如无法打开，只能写入 `03-缺口与待补充.md` 的备注，记录 URL、搜索摘要和失败原因；不得作为来源证据
- 没有找到明确依据的指标，要在 `03-缺口与待补充.md` 写明已检索渠道、关键词和当前边界
- 本 skill 只做搜索和写入文件，不做政策裁决

## 1. 文件用途

用于当输入 `{城市} + 购房资格 + 年份 + 标签` 时，检索住房限购、购房资格审核和相关补充通知。

## 2. 本文件结构

本文件作为“购房资格”内置模块文档，包含以下几段：

1. 文件用途
2. 渠道清单
3. 标签库
4. 检索词模板
5. 输出要求

## 3. 渠道清单模板

| 顺序 | 渠道编号 | 名称 | 可信度 | 扒取网址 | 主要用途 |
|---:|---|---|---:|---|---|
| 1 | `{CITY}-HOUSE-001` | `{城市}住房主管部门政策文件` | A | `{住建部门政策文件入口}` | 购房资格、住房限购、资格审核、房地产调控政策 |
| 2 | `{CITY}-HOUSE-002` | `{城市}住房主管部门官网` | A | `{住建部门官网}` | 最新调控通知、政策解读、住房交易管理 |
| 3 | `{CITY}-HOUSE-003` | `{城市}政府政策法规` | A | `{市政府政策法规入口}` | 市级政策原文、政府及部门通知 |
| 4 | `{CITY}-HOUSE-004` | `{城市}规范性文件库/通知公告` | A | `{规范性文件或通知入口}` | 有效期、废止、修订状态、现行有效判断 |
| 5 | `{CITY}-HOUSE-005` | `{城市}政府政策解读/政府公报` | B | `{政策解读或公报入口}` | 正式发布版本、历史文件、政策链核对 |
| 6 | `{CITY}-HOUSE-006` | `{省/市政务服务网}` | B | `{政务服务入口}` | 购房资格核验事项、材料、流程、办理入口 |
| 7 | `{CITY}-HOUSE-007` | `{城市}本地宝` | C | `{城市本地宝}` | 入口线索、常见问答、外链 |

补充说明：

- 实际执行时必须按本文件的渠道模板和顺序逐类检索，并用搜索工具定位目标城市真实页面
- 如果某类渠道无法定位真实页面，仍要记录已使用关键词和缺口，不自行跳过该渠道类型
- `A/B/C` 是模块文档内部的可信度分层，不等于最终表格里的“来源级别”

## 4. 标签库

| 标签编号 | 标签 | 检索词 | 重点输出字段 |
|---|---|---|---|
| HOUSE-001 | 购房资格 | 购房资格、购房资格审核、商品住房购房资格 | 资格条件、审核规则、适用对象 |
| HOUSE-002 | 限购政策 | 住房限购、商品住房限购、优化住房限购政策 | 限购范围、套数限制、区域差异 |
| HOUSE-003 | 非户籍条件 | 非本市户籍、社保、个税、连续缴纳 | 社保年限、个税年限、居住证要求 |
| HOUSE-004 | 家庭套数 | 家庭住房套数、家庭成员、单身、离异 | 家庭口径、套数认定、特殊人群 |
| HOUSE-005 | 审核流程 | 联网审核、资格审核、购房资格核验、申请材料 | 审核平台、材料、流程、时限 |
| HOUSE-006 | 政策有效期 | 有效期、废止、失效、调整、补充通知 | 现行有效状态、替代文件 |

## 5. 指标与关键词映射

| 指标编号 | 采集指标 | 优先关键词 | 重点抽取内容 |
|---|---|---|---|
| 1 | 是否需要当地社保 | 社保、非户籍、户籍家庭、人才购房、个税 | 必须区分本地户籍/非户籍、家庭/单身、人才引进/特殊人才、已婚/离异 |
| 2 | 社保缴纳年限要求（多少年/个月） | 连续缴纳、累计缴纳、个税、购房之日前连续 | 明确连续或累计、多少年/月、是否要求购房前连续、是否允许社保或个税满足条件 |
| 3 | 限购套数（首套 / 二套认定标准） | 限购套数、首套、二套、家庭住房套数、未成年子女 | 明确家庭名下住房数量、本市/全国住房口径、贷款记录、未成年子女名下住房是否计入 |

## 6. 对象与情形拆分提醒

遇到以下对象或情形时必须分别记录，不要混写：

- 本地户籍
- 非本地户籍
- 单身
- 家庭
- 人才引进/特殊人才
- 已婚
- 离异
- 商品住房
- 二手住房
- 普通住宅
- 非普通住宅
- 家庭购买
- 个人购买

## 7. 检索词模板

```text
site:{住建部门域名} {城市} 购房资格 {年份}
site:{住建部门域名} {城市} 住房限购 {年份}
site:{市政府域名} {城市} 商品住房 限购 {年份}
site:{市政府域名} {城市} 购房资格 审核
site:{本地宝域名} {城市} 购房资格 {年份}
```

## 8. 输出要求

- 按 `本地户籍/非户籍`、`家庭/单身`、`社保或个税条件`、`区域差异` 分开输出
- 必须把 `购房资格` 和 `住房贷款资格` 分开；贷款条件只能作为备注中的排除项，不能写进主结论
- 若同一页面同时提到购房资格、限购套数、审核流程，按指标拆开记录
- 若页面只写办事流程而未写资格条件，也要保留为有效来源，但备注要写清边界

## 9. 来源记录提醒

每条记录至少保留以下内容：

- 来源编号
- 页面标题
- 页面完整 URL
- URL 可访问性验证结果
- 发布日期或更新时间
- 抓取时间
- 检索关键词
- 搜索结果标题/摘要/排名
- 适用对象/适用购房情形
- 原文摘录
- 提取到的数据内容
- 对应指标编号
- 备注

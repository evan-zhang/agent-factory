# 07-落户-本科学历

本文件是“落户（本科学历前提）”模块的内置模块文档与字段约定，用于帮助 skill 理解常见渠道结构、人才引进与积分落户区分、社保口径、断缴补缴规则和积分规则。

真实执行时，本文件就是默认模块文档。执行代理必须先读本文件，并按本文件的渠道模板、标签和指标映射进行检索。这里的占位符用于生成目标城市查询，不是已采集来源。

## 检索工具要求

- 搜索检索优先使用 `MiniMax web_search`（via mcporter call minimax.web_search）
- 如果 MiniMax 不可用，回退到 `openclaw-tavily-search`（Tavily 搜索）
- 两者都不可用时，停止执行并说明缺少检索技能
- 实际使用哪个通道，须在来源明细的备注中记录
- 使用本文件中的渠道模板、渠道顺序和检索词模板构造查询
- `目标城市` 和 `年份/时间范围` 必须来自用户输入；搜索查询、来源明细和缺口记录都必须保留该年份口径
- 搜索工具只用于发现和定位页面；采集内容必须来自已打开页面
- 搜索结果摘要、片段或未打开页面不能当作官方原文
- 候选 URL 必须实际打开并验证可读；404、错误页、空白页、登录拦截页或不可读页面不能作为来源证据
- 采用来源时，`02-来源明细表.md` 的 `备注` 必须记录 URL 验证结果，以及 搜索结果标题/摘要/排名

## 与 SKILL.md 的配合方式

- 本 reference 不单独执行，必须配合上级 `SKILL.md` 使用
- `SKILL.md` 决定固定指标、输出文件、来源字段和追溯规则；本文件提供内置渠道模板、模块标签、关键词和指标映射
- 执行时先读本文件，再根据目标城市替换占位符并使用搜索工具定位真实官方页面
- 如用户额外提供城市材料，只能作为补充线索；不得替代本文件的渠道顺序、固定指标和输出约束

## 检索与存储提醒

- 对每个指标至少尝试使用对应关键词进行检索；找到相关页面后必须打开页面再采集
- 采用的每个页面都要写入 `02-来源明细表.md`，并保留完整 URL、URL 验证结果、检索关键词、搜索结果标题/摘要/排名、原文摘录和提取数据
- 搜索结果 URL 如无法打开，只能写入 `03-缺口与待补充.md` 的备注，记录 URL、搜索摘要和失败原因；不得作为来源证据
- 没有找到明确依据的指标，要在 `03-缺口与待补充.md` 写明已检索渠道、关键词和当前边界
- 本 skill 只做搜索和写入文件，不做政策裁决

## 1. 文件用途

用于当输入 `{城市} + 落户 + 本科 + 年份 + 标签` 时，检索人才引进、积分落户、毕业生接收和公安户籍迁入口径。

## 2. 本文件结构

本文件作为“落户（本科学历前提）”内置模块文档，包含以下几段：

1. 文件用途
2. 渠道清单
3. 标签库
4. 检索词模板
5. 输出要求

## 3. 渠道清单模板

| 顺序 | 渠道编号 | 名称 | 可信度 | 扒取网址 | 主要用途 |
|---:|---|---|---:|---|---|
| 1 | `{CITY}-SETTLE-001` | `{城市}人力资源和社会保障局官网/政策文件` | A | `{人社局入口}` | 人才引进、毕业生、社保条件、政策文件检索 |
| 2 | `{CITY}-SETTLE-002` | `{城市}公安局官网/户政入口` | A | `{公安局入口}` | 户政、户籍迁入、人口管理 |
| 3 | `{CITY}-SETTLE-003` | `{城市}公安局户籍迁入专题/信息公开` | B | `{户籍迁入专题}` | 户籍迁入、人才入户、材料、流程 |
| 4 | `{CITY}-SETTLE-004` | `{省/市政务服务网}` | B | `{政务服务入口}` | 入户事项、材料、流程 |
| 5 | `{CITY}-SETTLE-005` | `{城市}政府政策法规` | A | `{市政府政策法规入口}` | 市级政策原文 |
| 6 | `{CITY}-SETTLE-006` | `{城市}政府通知公告` | A | `{市政府通知公告入口}` | 最新调整 |
| 7 | `{CITY}-SETTLE-007` | `{城市}本地宝` | C | `{城市本地宝}` | 线索、问答、入口 |

补充说明：

- 实际执行时必须按本文件的渠道模板和顺序逐类检索，并用搜索工具定位目标城市真实页面
- 如果某类渠道无法定位真实页面，仍要记录已使用关键词和缺口，不自行跳过该渠道类型
- `A/B/C` 是模块文档内部的可信度分层，不等于最终表格里的“来源级别”

## 4. 标签库

| 标签编号 | 标签 | 检索词 | 重点输出字段 |
|---|---|---|---|
| SETTLE-001 | 本科落户 | 本科落户、本科入户、大学本科、学历条件 | 学历条件、年龄限制、社保要求 |
| SETTLE-002 | 人才引进 | 人才引进、人才落户、在职人才引进 | 申报条件、申报主体、材料 |
| SETTLE-003 | 应届毕业生 | 应届毕业生、毕业生落户、接收高校毕业生 | 应届条件、派遣/报到、办理流程 |
| SETTLE-004 | 户籍迁入 | 户籍迁入、户口迁入、准迁证、公共户 | 公安办理条件、材料、流程 |
| SETTLE-005 | 社保条件 | 社保、连续缴纳、参保状态、单位要求 | 缴纳年限、单位要求、状态要求 |
| SETTLE-006 | 积分落户 | 积分落户、居住证积分、积分指标 | 积分规则、申报时间、分值 |

## 5. 指标与关键词映射

| 指标编号 | 采集指标 | 优先关键词 | 重点抽取内容 |
|---|---|---|---|
| 1 | 本科学历是否可走人才引进直接落户 | 本科落户、人才引进、本科学历、年龄限制 | 必须区分“可申请人才引进”与“直接落户成功”，并保留附加条件 |
| 2 | 是否需要社保（作为落户条件之一） | 社保、参保、人才引进、积分落户 | 区分人才引进是否要求社保、积分落户是否要求社保、硬性门槛还是加分项 |
| 3 | 社保缴纳年限要求（多少年/个月） | 连续缴纳、累计缴纳、社保年限、申报时仍在缴纳 | 明确连续或累计、多少年/月、申报时是否仍在缴 |
| 4 | 断缴/补缴月份是否算入年限 | 断缴、补缴、有效月数、连续性 | 断缴是否中断连续性、补缴是否计入有效月数、不同落户路径是否不同 |
| 5 | 积分落户社保每年加分规则（每年加多少分） | 积分落户、社保加分、每年几分、险种 | 每年加多少分、是否按险种区分、是否必须本市社保、是否有封顶 |

## 6. 对象与落户路径拆分提醒

遇到以下对象或情形时必须分别记录，不要混写：

- 人才引进落户
- 积分落户
- 普通迁入/投靠落户
- 本科
- 硕士
- 博士
- 应届生
- 非应届生
- 年龄限制
- 单位引进
- 个人申报
- 市级规则
- 区级实施细则

## 7. 检索词模板

```text
site:{人社局域名} {城市} 本科 落户 {年份}
site:{人社局域名} {城市} 人才引进 本科 {年份}
site:{人社局域名} 应届毕业生 落户 {城市}
site:{公安局域名} {城市} 户籍迁入 本科
site:{政务服务域名} {城市} 落户 人才引进
site:{本地宝域名} {城市} 本科落户 {年份}
```

## 8. 输出要求

- 必须区分 `人才引进落户`、`积分落户`、`普通迁入/投靠落户`
- 不能简单输出“本科可落户/不可落户”，必须保留具体人群和附加条件
- 若页面只写办理流程而未写资格条件，也要保留为有效来源，但备注要写清边界
- 对积分落户，优先保留积分指标体系、实施办法、政策解读中的明确加分口径

## 9. 来源记录提醒

每条记录至少保留以下内容：

- 来源编号
- 页面标题
- 页面完整 URL
- URL 可访问性验证结果
- 发布日期或更新时间
- 抓取时间
- 检索关键词
- 搜索结果标题/摘要/排名
- 适用对象/适用落户路径
- 原文摘录
- 提取到的数据内容
- 对应指标编号
- 备注

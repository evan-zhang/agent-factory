# 01-异地就医

本文件是“异地就医”模块的内置模块文档与字段约定，用于帮助 skill 理解常见渠道结构、标签设计和指标映射。

真实执行时，本文件就是默认模块文档。执行代理必须先读本文件，并按本文件的渠道模板、标签和指标映射进行检索。这里的占位符用于生成目标城市查询，不是已采集来源。

## 检索工具要求

- 搜索检索优先使用 `MiniMax web_search`（via mcporter call minimax.web_search）
- 如果 MiniMax 不可用，回退到 `web_fetch`（网页抓取）
- 两者都不可用时，停止执行并说明缺少检索技能
- 实际使用哪个通道，须在来源明细的备注中记录
- 使用本文件中的渠道模板、渠道顺序和检索词模板构造查询
- `目标城市` 和 `年份/时间范围` 必须来自用户输入；搜索查询、来源明细和缺口记录都必须保留该年份口径
- 搜索工具只用于发现和定位页面；采集内容必须来自已打开页面
- 搜索结果摘要、片段或未打开页面不能当作官方原文
- 候选 URL 必须实际打开并验证可读；404、错误页、空白页、登录拦截页或不可读页面不能作为来源证据
- 采用来源时，`02-来源明细表.md` 的 `备注` 必须记录 URL 验证结果，以及 搜索结果标题/摘要/排名

## 与 SKILL.md 的配合方式

- 本 reference 不单独执行，必须配合上级 `SKILL.md` 使用
- `SKILL.md` 决定固定指标、输出文件、来源字段和追溯规则；本文件提供内置渠道模板、模块标签、关键词和指标映射
- 执行时先读本文件，再根据目标城市替换占位符并使用搜索工具定位真实官方页面
- 如用户额外提供城市材料，只能作为补充线索；不得替代本文件的渠道顺序、固定指标和输出约束

## 检索与存储提醒

- 对每个指标至少尝试使用对应关键词进行检索；找到相关页面后必须打开页面再采集
- 采用的每个页面都要写入 `02-来源明细表.md`，并保留完整 URL、URL 验证结果、检索关键词、搜索结果标题/摘要/排名、原文摘录和提取数据
- 搜索结果 URL 如无法打开，只能写入 `03-缺口与待补充.md` 的备注，记录 URL、搜索摘要和失败原因；不得作为来源证据
- 没有找到明确依据的指标，要在 `03-缺口与待补充.md` 写明已检索渠道、关键词和当前边界
- 本 skill 只做搜索和写入文件，不做政策裁决

## 1. 文件用途

用于当输入 `{城市} + 异地就医 + 年份 + 标签` 时，检索该城市医保异地就医政策、专题说明、办事指南和跨省直接结算规则。

## 2. 本文件结构

本文件作为“异地就医”内置模块文档，包含以下几段：

1. 文件用途
2. 渠道清单
3. 标签库
4. 检索词模板
5. 输出要求

## 3. 渠道清单模板

| 顺序 | 渠道编号 | 名称 | 可信度 | 扒取网址 | 主要用途 |
|---:|---|---|---:|---|---|
| 1 | `{CITY}-MED-001` | `{城市}市医疗保障局官网` | A | `{城市医保局官网}` | 医保政策、通知公告、政策解读 |
| 2 | `{CITY}-MED-002` | `{城市}医保异地就医专题` | B | `{异地就医专题}` | 异地备案、直接结算、参保地和就医地口径 |
| 3 | `{CITY}-MED-003` | `{省/市政务服务网}` | B | `{政务服务入口}` | 异地就医备案事项、材料、流程、办理入口 |
| 4 | `{CITY}-MED-004` | `{城市}市政府政策法规` | A | `{市政府政策法规入口}` | 市级政策原文、规范性文件 |
| 5 | `{CITY}-MED-005` | `{城市}市政府政策解读` | B | `{市政府政策解读入口}` | 政策解释、问答 |
| 6 | `{CITY}-MED-006` | `全国医保服务平台` | B | `https://fuwu.nhsa.gov.cn/` | 跨省异地备案、联网定点机构核对 |
| 7 | `{CITY}-MED-007` | `{城市}本地宝` | C | `{城市本地宝}` | 线索、入口、常见问答 |

补充说明：

- 实际执行时必须按本文件的渠道模板和顺序逐类检索，并用搜索工具定位目标城市真实页面
- 如果某类渠道无法定位真实页面，仍要记录已使用关键词和缺口，不自行跳过该渠道类型
- `A/B/C` 是模块文档内部的可信度分层，不等于最终表格里的“来源级别”

## 4. 标签库

| 标签编号 | 标签 | 检索词 | 重点输出字段 |
|---|---|---|---|
| MED-001 | 备案 | 异地就医备案、跨省异地就医备案、线上备案 | 备案对象、备案方式、备案有效期 |
| MED-002 | 直接结算 | 直接结算、跨省直接结算、联网结算 | 支持范围、定点机构、限制条件 |
| MED-003 | 参保地政策 | 参保地政策、参保地待遇、报销比例、起付线 | 待遇标准、起付线、封顶线 |
| MED-004 | 就医地目录 | 就医地目录、药品目录、诊疗项目 | 目录适用规则、限制说明 |
| MED-005 | 门诊慢特病 | 门诊慢特病、门诊特殊病、慢病异地结算 | 病种范围、备案要求、结算规则 |
| MED-006 | 手工报销 | 手工报销、现金报销、零星报销 | 材料、期限、流程 |

## 5. 指标与关键词映射

| 指标编号 | 采集指标 | 优先关键词 | 重点抽取内容 |
|---|---|---|---|
| 1 | 门诊能否报销 | 门诊、普通门诊、门诊统筹、异地就医直接结算 | 是否支持门诊跨省结算、适用门诊类型、前置条件 |
| 2 | 住院能否报销 | 住院、跨省住院、直接结算 | 是否支持住院跨省结算、联网定点要求 |
| 3 | 社康/社区医院能否报销 | 社康、社区医院、基层医疗机构、一级医院 | 是否纳入结算范围、是否需要联网定点 |
| 4 | 门诊报销比例（%） | 报销比例、起付线、门诊待遇、参保地政策 | 明确比例、医院等级差异、自付段 |
| 5 | 住院报销比例（%） | 住院待遇、报销比例、起付标准 | 明确比例、起付线、不同医院级别差异 |
| 6 | 年度报销额度上限（元） | 封顶线、最高支付限额、年度限额 | 明确额度、适用年度、适用险种 |
| 7 | 家庭共济账户是否可用（父母/配偶/子女） | 家庭共济、个人账户共济、父母、配偶、子女 | 是否可用、可用范围、使用条件 |
| 8 | 备案有效期 + 备案操作方式 | 备案有效期、线上备案、APP、小程序、窗口办理 | 有效期、办理入口、是否即时生效 |
| 9 | 结算方式（直接结算 vs 先垫付再报销） | 直接结算、先行垫付、手工报销、零星报销 | 结算路径、何种情形需垫付 |
| 10 | 未备案自行就医报销比例是否下调 | 未备案、自行就医、降低支付比例、下调比例 | 是否下调、下调幅度、是否有例外 |
| 11 | 异地急诊住院能否报销 | 急诊、抢救、异地急诊住院、自动备案 | 是否可报销、是否视同备案、结算方式 |

## 6. 检索词模板

```text
site:{医保局域名} {城市} 异地就医 {标签} {年份}
site:{医保局域名} 跨省异地就医 直接结算 {年份}
site:{异地就医专题域名} 异地就医备案
site:{政务服务域名} {城市} 异地就医备案
site:{市政府域名} {城市} 异地就医 {年份}
site:fuwu.nhsa.gov.cn {城市} 跨省异地就医 {标签}
site:{本地宝域名} {城市} 异地就医 {标签} {年份}
```

## 7. 输出要求

- 优先采用医保局官网和异地就医专题页
- 政务服务网主要用于材料、流程和办理入口补充
- 市政府政策法规和政策解读用于补政策链
- 全国医保服务平台用于核对备案入口和联网定点
- 本地宝只作为线索和辅助，不作为最终政策依据

## 8. 来源记录提醒

每条记录至少保留以下内容：

- 来源编号
- 页面标题
- 页面完整 URL
- URL 可访问性验证结果
- 发布日期或更新时间
- 抓取时间
- 检索关键词
- 搜索结果标题/摘要/排名
- 原文摘录
- 提取到的数据内容
- 对应指标编号
- 备注

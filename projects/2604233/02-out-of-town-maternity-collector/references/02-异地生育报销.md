# 02-异地生育报销

本文件是“异地生育报销”模块的内置模块文档与字段约定，用于帮助 skill 理解常见渠道结构、标签设计、对象区分和指标映射。

真实执行时，本文件就是默认模块文档。执行代理必须先读本文件，并按本文件的渠道模板、标签和指标映射进行检索。这里的占位符用于生成目标城市查询，不是已采集来源。

## 数据采集要求

- 采集内容必须来自已打开的官方页面，搜索结果摘要不能当作官方原文
- 搜索工具按 EXECUTION-PROMPT Phase 0 探测结果选择（优先 MiniMax web_search）
- 实际使用的搜索工具和抓取工具，须在来源明细的备注中记录
- 遇到 JS 渲染页面无法采集时，在缺口表中标注原因（JS渲染失败）
- 搜索不到目标时，先尝试不同关键词组合和 site: 限定，再标记缺口
- 使用本文件中的渠道模板、渠道顺序和检索词模板构造查询
- `目标城市` 和 `年份/时间范围` 必须来自用户输入；搜索查询、来源明细和缺口记录都必须保留该年份口径
- 搜索工具只用于发现和定位页面；采集内容必须来自已打开页面
- 搜索结果摘要、片段或未打开页面不能当作官方原文
- 候选 URL 必须实际打开并验证可读；404、错误页、空白页、登录拦截页或不可读页面不能作为来源证据
- 采用来源时，`02-来源明细表.md` 的 `备注` 必须记录 URL 验证结果，以及 搜索结果标题/摘要/排名

## 与 SKILL.md 的配合方式

- 本 reference 不单独执行，必须配合上级 `SKILL.md` 使用
- `SKILL.md` 决定固定指标、输出文件、来源字段和追溯规则；本文件提供内置渠道模板、模块标签、关键词和指标映射
- 执行时先读本文件，再根据目标城市替换占位符并使用搜索工具定位真实官方页面
- 如用户额外提供城市材料，只能作为补充线索；不得替代本文件的渠道顺序、固定指标和输出约束

## 检索与存储提醒

- 对每个指标至少尝试使用对应关键词进行检索；找到相关页面后必须打开页面再采集
- 采用的每个页面都要写入 `02-来源明细表.md`，并保留完整 URL、URL 验证结果、检索关键词、搜索结果标题/摘要/排名、原文摘录和提取数据
- 搜索结果 URL 如无法打开，只能写入 `03-缺口与待补充.md` 的备注，记录 URL、搜索摘要和失败原因；不得作为来源证据
- 没有找到明确依据的指标，要在 `03-缺口与待补充.md` 写明已检索渠道、关键词和当前边界
- 本 skill 只做搜索和写入文件，不做政策裁决

## 1. 文件用途

用于当输入 `{城市} + 异地生育报销 + 年份 + 标签` 时，检索该城市医保、生育保险、政务服务事项和异地结算规则。

## 2. 本文件结构

本文件作为“异地生育报销”内置模块文档，包含以下几段：

1. 文件用途
2. 渠道清单
3. 标签库
4. 检索词模板
5. 输出要求

## 3. 渠道清单模板

| 顺序 | 渠道编号 | 名称 | 可信度 | 扒取网址 | 主要用途 |
|---:|---|---|---:|---|---|
| 1 | `{CITY}-MAT-001` | `{城市}市医疗保障局官网` | A | `{城市医保局官网}` | 生育保险、生育医疗费用、医保待遇 |
| 2 | `{CITY}-MAT-002` | `{省/市政务服务网}` | B | `{政务服务入口}` | 生育医疗费用报销事项、材料、流程 |
| 3 | `{CITY}-MAT-003` | `{城市}市政府政策法规` | A | `{市政府政策法规入口}` | 市级政策原文 |
| 4 | `{CITY}-MAT-004` | `{城市}市政府通知公告` | A | `{市政府通知公告入口}` | 最新通知、阶段性调整 |
| 5 | `{CITY}-MAT-005` | `{城市}市政府政策解读` | B | `{市政府政策解读入口}` | 政策解读、问答 |
| 6 | `{CITY}-MAT-006` | `{城市}市人力资源和社会保障局` | A | `{人社局入口}` | 生育津贴、社保关联口径辅助核验 |
| 7 | `{CITY}-MAT-007` | `{城市}本地宝` | C | `{城市本地宝}` | 线索、入口、常见问答 |

补充说明：

- 实际执行时必须按本文件的渠道模板和顺序逐类检索，并用搜索工具定位目标城市真实页面
- 如果某类渠道无法定位真实页面，仍要记录已使用关键词和缺口，不自行跳过该渠道类型
- `A/B/C` 是模块文档内部的可信度分层，不等于最终表格里的“来源级别”

## 4. 标签库

| 标签编号 | 标签 | 检索词 | 重点输出字段 |
|---|---|---|---|
| MAT-001 | 异地生育备案 | 异地生育备案、省内异地生育、省外异地生育 | 是否需要备案、备案方式、备案时间 |
| MAT-002 | 生育医疗费用 | 生育医疗费用报销、产前检查、分娩、计划生育医疗费用 | 报销范围、待遇标准、限制条件 |
| MAT-003 | 生育津贴 | 生育津贴、产假津贴、津贴申领 | 申领条件、计算口径、办理材料 |
| MAT-004 | 报销材料 | 材料清单、票据、诊断证明、出生医学证明 | 材料名称、特殊要求 |
| MAT-005 | 报销期限 | 申请期限、办理期限、报销时限 | 申请截止时间、办理时限 |
| MAT-006 | 联网结算 | 生育联网结算、省内联网结算、直接结算 | 是否支持、适用医院、结算流程 |

## 5. 指标与关键词映射

| 指标编号 | 采集指标 | 优先关键词 | 重点抽取内容 |
|---|---|---|---|
| 1 | 能否在{城市}报销 | 异地生育报销、生育医疗费用报销、异地分娩 | 是否支持异地报销、是否区分省内省外、是否区分已备案/未备案 |
| 2 | 报销方式（直连结算 vs 垫付后报销） | 直接结算、联网结算、手工报销、现金报销 | 能否联网结算、哪些情形需先垫付、回参保地报销路径 |
| 3 | 报销额度（定额标准/比例/上限） | 定额标准、报销比例、产前检查、分娩、计划生育手术、封顶线 | 明确额度、不同项目差异、不同分娩方式差异、是否目录内按比例支付 |
| 4 | 是否需要提前备案 | 生育备案、异地生育备案、备案材料、备案流程 | 是否强制备案、备案时间、备案入口、备案材料 |
| 5 | 生育津贴是否受影响 | 生育津贴、产假津贴、津贴标准、异地生育 | 津贴是否受异地分娩影响、申领条件、核发标准 |
| 6 | 计划生育手术（流产/引产等）能否报销 | 流产、引产、宫外孕、放环、取环、计划生育手术 | 是否纳入生育待遇、适用对象、支付方式、支付标准 |

## 6. 适用对象拆分提醒

遇到以下对象时必须分别记录，不要混写：

- 女职工本人
- 男职工配偶
- 未就业配偶
- 灵活就业人员

## 7. 情形拆分提醒

遇到以下情形时必须分别记录，不要混写：

- 本地生育
- 异地生育
- 已备案
- 未备案
- 直接结算
- 先垫付后报销
- 产前检查
- 住院分娩
- 生育并发症
- 计划生育手术

## 8. 检索词模板

```text
site:{医保局域名} {城市} 异地生育备案 {年份}
site:{医保局域名} 生育医疗费用报销 异地 {年份}
site:{政务服务域名} {城市} 生育保险 医疗费用报销
site:{市政府域名} {城市} 生育保险 报销 {年份}
site:{人社局域名} {城市} 生育津贴 {年份}
site:{本地宝域名} {城市} 异地生育报销 {年份}
```

## 9. 输出要求

- 把 `生育医疗费用报销`、`生育津贴`、`异地备案/联网结算` 分开输出，不合并成单一结论
- 若同一页面同时提到分娩、产检、流产、放取环等，按指标和情形拆开记录
- 若页面只写办理流程而未写待遇标准，也要保留为有效来源，但备注要写清边界

## 10. 来源记录提醒

每条记录至少保留以下内容：

- 来源编号
- 页面标题
- 页面完整 URL
- URL 可访问性验证结果
- 发布日期或更新时间
- 抓取时间
- 检索关键词
- 搜索结果标题/摘要/排名
- 适用对象
- 原文摘录
- 提取到的数据内容
- 对应指标编号
- 备注

# 03-公积金异地购房贷款

本文件是“公积金异地购房贷款”模块的内置模块文档与字段约定，用于帮助 skill 理解常见渠道结构、方向区分、房屋情形区分和指标映射。

真实执行时，本文件就是默认模块文档。执行代理必须先读本文件，并按本文件的渠道模板、标签和指标映射进行检索。这里的占位符用于生成目标城市查询，不是已采集来源。

## 检索工具要求

- 搜索检索优先使用 `MiniMax web_search`（via mcporter call minimax.web_search）
- 如果 MiniMax 不可用，回退到 `openclaw-tavily-search`（Tavily 搜索）
- 两者都不可用时，停止执行并说明缺少检索技能
- 实际使用哪个通道，须在来源明细的备注中记录
- 使用本文件中的渠道模板、渠道顺序和检索词模板构造查询
- `目标城市` 和 `年份/时间范围` 必须来自用户输入；搜索查询、来源明细和缺口记录都必须保留该年份口径
- 搜索工具只用于发现和定位页面；采集内容必须来自已打开页面
- 搜索结果摘要、片段或未打开页面不能当作官方原文
- 候选 URL 必须实际打开并验证可读；404、错误页、空白页、登录拦截页或不可读页面不能作为来源证据
- 采用来源时，`02-来源明细表.md` 的 `备注` 必须记录 URL 验证结果，以及 搜索结果标题/摘要/排名

## 与 SKILL.md 的配合方式

- 本 reference 不单独执行，必须配合上级 `SKILL.md` 使用
- `SKILL.md` 决定固定指标、输出文件、来源字段和追溯规则；本文件提供内置渠道模板、模块标签、关键词和指标映射
- 执行时先读本文件，再根据目标城市替换占位符并使用搜索工具定位真实官方页面
- 如用户额外提供城市材料，只能作为补充线索；不得替代本文件的渠道顺序、固定指标和输出约束

## 检索与存储提醒

- 对每个指标至少尝试使用对应关键词进行检索；找到相关页面后必须打开页面再采集
- 采用的每个页面都要写入 `02-来源明细表.md`，并保留完整 URL、URL 验证结果、检索关键词、搜索结果标题/摘要/排名、原文摘录和提取数据
- 搜索结果 URL 如无法打开，只能写入 `03-缺口与待补充.md` 的备注，记录 URL、搜索摘要和失败原因；不得作为来源证据
- 没有找到明确依据的指标，要在 `03-缺口与待补充.md` 写明已检索渠道、关键词和当前边界
- 本 skill 只做搜索和写入文件，不做政策裁决

## 1. 文件用途

用于当输入 `{城市} + 公积金异地购房贷款 + 年份 + 标签` 时，检索该城市公积金贷款政策、异地缴存规则、贷款条件和办理流程。

## 2. 本文件结构

本文件作为“公积金异地购房贷款”内置模块文档，包含以下几段：

1. 文件用途
2. 渠道清单
3. 标签库
4. 检索词模板
5. 输出要求

## 3. 渠道清单模板

| 顺序 | 渠道编号 | 名称 | 可信度 | 扒取网址 | 主要用途 |
|---:|---|---|---:|---|---|
| 1 | `{CITY}-FUND-001` | `{城市}住房公积金主管部门政策文件` | A | `{公积金政策文件入口}` | 公积金政策原文、中心文件、管委会文件 |
| 2 | `{CITY}-FUND-002` | `{城市}住房公积金贷款政策栏目/专题` | B | `{贷款政策栏目}` | 贷款条件、贷款额度、异地缴存、缴存证明 |
| 3 | `{CITY}-FUND-003` | `{城市}住房公积金主管部门官网` | A | `{公积金中心官网}` | 办事指南、贷款经办部门、表格下载、政策解读 |
| 4 | `{CITY}-FUND-004` | `{省/市政务服务网}` | B | `{政务服务入口}` | 公积金贷款事项办理材料、流程、办理入口 |
| 5 | `{CITY}-FUND-005` | `{城市}政府政策法规` | A | `{市政府政策法规入口}` | 市级政策原文、部门联合发文 |
| 6 | `{CITY}-FUND-006` | `国家政务服务平台` | B | `https://gjzwfw.www.gov.cn/` | 跨地区事项兜底、事项名称核对 |
| 7 | `{CITY}-FUND-007` | `{城市}本地宝` | C | `{城市本地宝}` | 线索、入口、常见问答 |

补充说明：

- 实际执行时必须按本文件的渠道模板和顺序逐类检索，并用搜索工具定位目标城市真实页面
- 如果某类渠道无法定位真实页面，仍要记录已使用关键词和缺口，不自行跳过该渠道类型
- `A/B/C` 是模块文档内部的可信度分层，不等于最终表格里的“来源级别”

## 4. 标签库

| 标签编号 | 标签 | 检索词 | 重点输出字段 |
|---|---|---|---|
| FUND-001 | 异地贷款 | 住房公积金异地贷款、异地购房贷款、异地缴存贷款 | 是否支持、适用范围、贷款条件 |
| FUND-002 | 缴存证明 | 缴存使用证明、异地贷款职工住房公积金缴存使用证明 | 证明名称、开具方式、有效期 |
| FUND-003 | 贷款额度 | 可贷额度、贷款额度、最高额度、额度计算 | 额度公式、最高额度、影响因素 |
| FUND-004 | 首付比例 | 首付款比例、首套房、二套房、贷款成数 | 首付比例、住房套数口径 |
| FUND-005 | 贷款材料 | 贷款材料、申请材料、购房合同、收入证明 | 材料清单、办理流程 |
| FUND-006 | 办理流程 | 贷款申请、贷款审批、受理网点、办理流程 | 流程步骤、办理入口、时限 |

## 5. 指标与关键词映射

| 指标编号 | 采集指标 | 优先关键词 | 重点抽取内容 |
|---|---|---|---|
| 1 | 是否支持异地公积金贷款 | 异地缴存、公积金异地贷款、购房贷款、异地互认 | 必须区分“异地缴存，在本市申请贷款”和“本市缴存，去外地购房贷款” |
| 2 | 贷款额度上限（首套 / 二套，万元） | 贷款额度、最高额度、首套、二套、家庭申请、个人申请 | 明确首套/二套、家庭/个人差异，是否与余额、基数、还款能力挂钩 |
| 3 | 贷款利率（首套 / 二套，%） | 公积金贷款利率、首套利率、二套利率、中国人民银行 | 明确首套/二套利率，是否引用全国统一利率政策 |
| 4 | 最低缴存时长要求（连续缴存多少个月） | 连续缴存、正常缴存、缴存月数、贷款条件 | 连续缴存月数、正常缴存状态、申请时是否仍需缴存 |
| 5 | 是否需要先签购房合同才能申请贷款 | 购房合同、贷款申请、资格预审、提交材料 | 区分资格咨询/预审、正式申贷、提交资料三个阶段 |

## 6. 方向拆分提醒

遇到以下两类方向时必须分别记录，不要混写：

- 异地缴存，在本市申请贷款
- 本市缴存，去外地购房贷款

## 7. 房屋与申请情形拆分提醒

遇到以下情形时必须分别记录，不要混写：

- 新房
- 二手房
- 首套
- 二套
- 本地户籍
- 非户籍
- 单人申请
- 共同申请

## 8. 检索词模板

```text
site:{公积金中心官网域名} {城市} 住房公积金 异地贷款 {年份}
site:{公积金中心官网域名} 异地贷款职工住房公积金缴存使用证明
site:{公积金中心官网域名} 公积金贷款额度 {年份}
site:{政务服务域名} {城市} 公积金贷款
site:{市政府域名} {城市} 住房公积金 异地贷款
site:{本地宝域名} {城市} 公积金异地贷款 {年份}
```

## 9. 输出要求

- 必须区分 `政策是否支持异地贷款`、`贷款额度计算`、`贷款利率`、`材料流程` 四类信息，不能混成一个结论
- 若同一页面同时提到额度、利率、材料和申请流程，按指标拆开记录
- 若页面只写办事流程而未写额度、利率或时长，也要保留为有效来源，但备注要写清边界

## 10. 来源记录提醒

每条记录至少保留以下内容：

- 来源编号
- 页面标题
- 页面完整 URL
- URL 可访问性验证结果
- 发布日期或更新时间
- 抓取时间
- 检索关键词
- 搜索结果标题/摘要/排名
- 适用对象/适用房屋情形
- 原文摘录
- 提取到的数据内容
- 对应指标编号
- 备注
